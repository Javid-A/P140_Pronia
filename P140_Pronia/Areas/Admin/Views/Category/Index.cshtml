@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
}

<div class="table pt-3">
    <table class="table table-dark">
        <thead>
            <tr>
                <th>
                    #
                </th>
                <th>
                    Category name
                </th>      
                <th>
                    Settings
                </th>
            </tr>
        </thead>
        <tbody>
            @{
                int count = 0;
            }
         @foreach (Category category in Model)
            {
                <tr>
                    <td>
                        @(++count)
                    </td>
                    <td>
                        @category.Name
                    </td>
                    <td>
                      <div>
                            <a asp-controller="Category" asp-action="Detail" asp-route-id="@category.Id" class="btn btn-info btn-rounded btn-fw">Details</a>
                            <a asp-controller="Category" asp-action="Create" class="btn btn-success btn-rounded btn-fw">Create</a>
                            <a asp-controller="Category" asp-action="Update"  class="btn btn-warning btn-rounded btn-fw">Update</a>
                            <a asp-controller="Category" asp-action="Delete"  class="btn btn-danger btn-rounded btn-fw">Delete</a>
                      </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>